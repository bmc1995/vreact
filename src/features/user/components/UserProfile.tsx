import { Card, CardContent, CardCover, Typography } from "@mui/joy";

export default function UserProfile() {
  return (
    <Card title="User Profile Card">
      <CardCover>
        <CardContent>
          <Typography>User Profile Works!</Typography>
        </CardContent>
      </CardCover>
    </Card>
  );
}
