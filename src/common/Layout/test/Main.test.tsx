import { describe, it } from "vitest";

describe("<Main />", () => {
  it.skip("Main tests not defined");
  it.skip("renders Main component");
  it.skip("renders child components");
  it.skip("Is a `<main />` HTML Element (Semantic HTML) ");
});
